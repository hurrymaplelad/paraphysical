name: Deno Check and Test
runs:
  using: composite
  steps:
    - uses: denoland/setup-deno@v1
      with:
        deno-version: v1.x
    - run: deno fmt --check
      shell: bash
    - run: deno test --allow-read
      shell: bash
